export { JwtAuthGuard } from './guards/jwt-auth.guard';
export { LocalAuthGuard } from './guards/local-auth.guard';

export { LoginUserDto } from '@project/common';
export { CreateUserDto } from '@project/common';
export { ChangePasswordUserDto } from '@project/common';

export { AuthenticationModule } from './authentication-module/authentication.module';
